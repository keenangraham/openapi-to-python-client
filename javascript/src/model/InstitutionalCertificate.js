/**
 * IGVF Project API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The InstitutionalCertificate model module.
 * @module model/InstitutionalCertificate
 * @version 0.1.0
 */
class InstitutionalCertificate {
    /**
     * Constructs a new <code>InstitutionalCertificate</code>.
     * An institutional certificate defining the data sharing limitations for data authorized for submission to the IGVF portal.
     * @alias module:model/InstitutionalCertificate
     */
    constructor() { 
        
        InstitutionalCertificate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>InstitutionalCertificate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InstitutionalCertificate} obj Optional instance to populate.
     * @return {module:model/InstitutionalCertificate} The populated <code>InstitutionalCertificate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InstitutionalCertificate();

            if (data.hasOwnProperty('release_timestamp')) {
                obj['release_timestamp'] = ApiClient.convertToType(data['release_timestamp'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('lab')) {
                obj['lab'] = ApiClient.convertToType(data['lab'], 'String');
            }
            if (data.hasOwnProperty('award')) {
                obj['award'] = ApiClient.convertToType(data['award'], 'String');
            }
            if (data.hasOwnProperty('schema_version')) {
                obj['schema_version'] = ApiClient.convertToType(data['schema_version'], 'String');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('aliases')) {
                obj['aliases'] = ApiClient.convertToType(data['aliases'], ['String']);
            }
            if (data.hasOwnProperty('creation_timestamp')) {
                obj['creation_timestamp'] = ApiClient.convertToType(data['creation_timestamp'], 'String');
            }
            if (data.hasOwnProperty('submitted_by')) {
                obj['submitted_by'] = ApiClient.convertToType(data['submitted_by'], 'String');
            }
            if (data.hasOwnProperty('submitter_comment')) {
                obj['submitter_comment'] = ApiClient.convertToType(data['submitter_comment'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('certificate_identifier')) {
                obj['certificate_identifier'] = ApiClient.convertToType(data['certificate_identifier'], 'String');
            }
            if (data.hasOwnProperty('controlled_access')) {
                obj['controlled_access'] = ApiClient.convertToType(data['controlled_access'], 'Boolean');
            }
            if (data.hasOwnProperty('data_use_limitation')) {
                obj['data_use_limitation'] = ApiClient.convertToType(data['data_use_limitation'], 'String');
            }
            if (data.hasOwnProperty('data_use_limitation_modifiers')) {
                obj['data_use_limitation_modifiers'] = ApiClient.convertToType(data['data_use_limitation_modifiers'], ['String']);
            }
            if (data.hasOwnProperty('samples')) {
                obj['samples'] = ApiClient.convertToType(data['samples'], ['String']);
            }
            if (data.hasOwnProperty('urls')) {
                obj['urls'] = ApiClient.convertToType(data['urls'], ['String']);
            }
            if (data.hasOwnProperty('@id')) {
                obj['@id'] = ApiClient.convertToType(data['@id'], 'String');
            }
            if (data.hasOwnProperty('@type')) {
                obj['@type'] = ApiClient.convertToType(data['@type'], ['String']);
            }
            if (data.hasOwnProperty('summary')) {
                obj['summary'] = ApiClient.convertToType(data['summary'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InstitutionalCertificate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InstitutionalCertificate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['release_timestamp'] && !(typeof data['release_timestamp'] === 'string' || data['release_timestamp'] instanceof String)) {
            throw new Error("Expected the field `release_timestamp` to be a primitive type in the JSON string but got " + data['release_timestamp']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['lab'] && !(typeof data['lab'] === 'string' || data['lab'] instanceof String)) {
            throw new Error("Expected the field `lab` to be a primitive type in the JSON string but got " + data['lab']);
        }
        // ensure the json data is a string
        if (data['award'] && !(typeof data['award'] === 'string' || data['award'] instanceof String)) {
            throw new Error("Expected the field `award` to be a primitive type in the JSON string but got " + data['award']);
        }
        // ensure the json data is a string
        if (data['schema_version'] && !(typeof data['schema_version'] === 'string' || data['schema_version'] instanceof String)) {
            throw new Error("Expected the field `schema_version` to be a primitive type in the JSON string but got " + data['schema_version']);
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['aliases'])) {
            throw new Error("Expected the field `aliases` to be an array in the JSON data but got " + data['aliases']);
        }
        // ensure the json data is a string
        if (data['creation_timestamp'] && !(typeof data['creation_timestamp'] === 'string' || data['creation_timestamp'] instanceof String)) {
            throw new Error("Expected the field `creation_timestamp` to be a primitive type in the JSON string but got " + data['creation_timestamp']);
        }
        // ensure the json data is a string
        if (data['submitted_by'] && !(typeof data['submitted_by'] === 'string' || data['submitted_by'] instanceof String)) {
            throw new Error("Expected the field `submitted_by` to be a primitive type in the JSON string but got " + data['submitted_by']);
        }
        // ensure the json data is a string
        if (data['submitter_comment'] && !(typeof data['submitter_comment'] === 'string' || data['submitter_comment'] instanceof String)) {
            throw new Error("Expected the field `submitter_comment` to be a primitive type in the JSON string but got " + data['submitter_comment']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['certificate_identifier'] && !(typeof data['certificate_identifier'] === 'string' || data['certificate_identifier'] instanceof String)) {
            throw new Error("Expected the field `certificate_identifier` to be a primitive type in the JSON string but got " + data['certificate_identifier']);
        }
        // ensure the json data is a string
        if (data['data_use_limitation'] && !(typeof data['data_use_limitation'] === 'string' || data['data_use_limitation'] instanceof String)) {
            throw new Error("Expected the field `data_use_limitation` to be a primitive type in the JSON string but got " + data['data_use_limitation']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['data_use_limitation_modifiers'])) {
            throw new Error("Expected the field `data_use_limitation_modifiers` to be an array in the JSON data but got " + data['data_use_limitation_modifiers']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['samples'])) {
            throw new Error("Expected the field `samples` to be an array in the JSON data but got " + data['samples']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['urls'])) {
            throw new Error("Expected the field `urls` to be an array in the JSON data but got " + data['urls']);
        }
        // ensure the json data is a string
        if (data['@id'] && !(typeof data['@id'] === 'string' || data['@id'] instanceof String)) {
            throw new Error("Expected the field `@id` to be a primitive type in the JSON string but got " + data['@id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['@type'])) {
            throw new Error("Expected the field `@type` to be an array in the JSON data but got " + data['@type']);
        }
        // ensure the json data is a string
        if (data['summary'] && !(typeof data['summary'] === 'string' || data['summary'] instanceof String)) {
            throw new Error("Expected the field `summary` to be a primitive type in the JSON string but got " + data['summary']);
        }

        return true;
    }


}



/**
 * The date the object was released.
 * @member {String} release_timestamp
 */
InstitutionalCertificate.prototype['release_timestamp'] = undefined;

/**
 * The status of the metadata object.
 * @member {module:model/InstitutionalCertificate.StatusEnum} status
 */
InstitutionalCertificate.prototype['status'] = undefined;

/**
 * Lab associated with the submission.
 * @member {String} lab
 */
InstitutionalCertificate.prototype['lab'] = undefined;

/**
 * Grant associated with the submission.
 * @member {String} award
 */
InstitutionalCertificate.prototype['award'] = undefined;

/**
 * The version of the JSON schema that the server uses to validate the object.
 * @member {String} schema_version
 */
InstitutionalCertificate.prototype['schema_version'] = undefined;

/**
 * The unique identifier associated with every object.
 * @member {String} uuid
 */
InstitutionalCertificate.prototype['uuid'] = undefined;

/**
 * DACC internal notes.
 * @member {String} notes
 */
InstitutionalCertificate.prototype['notes'] = undefined;

/**
 * Lab specific identifiers to reference an object.
 * @member {Array.<String>} aliases
 */
InstitutionalCertificate.prototype['aliases'] = undefined;

/**
 * The date the object was created.
 * @member {String} creation_timestamp
 */
InstitutionalCertificate.prototype['creation_timestamp'] = undefined;

/**
 * The user who submitted the object.
 * @member {String} submitted_by
 */
InstitutionalCertificate.prototype['submitted_by'] = undefined;

/**
 * Additional information specified by the submitter to be displayed as a comment on the portal.
 * @member {String} submitter_comment
 */
InstitutionalCertificate.prototype['submitter_comment'] = undefined;

/**
 * A plain text description of the object.
 * @member {String} description
 */
InstitutionalCertificate.prototype['description'] = undefined;

/**
 * A unique identifier for the certificate.
 * @member {String} certificate_identifier
 */
InstitutionalCertificate.prototype['certificate_identifier'] = undefined;

/**
 * Indicator of whether the samples are under controlled access.
 * @member {Boolean} controlled_access
 */
InstitutionalCertificate.prototype['controlled_access'] = undefined;

/**
 * Code indicating the limitations on data use for data generated from the applicable samples. GRU (General research use): Use of the data is limited only by the terms of the Data Use Certification: these data will be added to the dbGaP Collection. HMB (Health/medical/biomedical): Use of the data is limited to health/medical/biomedical purposes, does not include the study of population origins or ancestry. DS (Disease specific): Use of the data must be related to the specified disease. Other: any other customized limitation.
 * @member {module:model/InstitutionalCertificate.DataUseLimitationEnum} data_use_limitation
 */
InstitutionalCertificate.prototype['data_use_limitation'] = undefined;

/**
 * Code indicating a modifier on the limitations on data use for data generated from the applicable samples. COL: Requestor must provide a letter of collaboration with the primary study investigator(s). GSO: Use of the data is limited to genetic studies only. IRB: Approval Required IRB Requestor must provide documentation of local IRB approval. MDS: Use of the data includes methods development research (e.g., development and testing of software or algorithms). NPU: Use of the data is limited to not-for-profit organizations. PUB: Requestor agrees to make results of studies using the data available to the larger scientific community.
 * @member {Array.<module:model/InstitutionalCertificate.DataUseLimitationModifiersEnum>} data_use_limitation_modifiers
 */
InstitutionalCertificate.prototype['data_use_limitation_modifiers'] = undefined;

/**
 * Samples covered by this institutional certificate.
 * @member {Array.<String>} samples
 */
InstitutionalCertificate.prototype['samples'] = undefined;

/**
 * Link to the institutional certification form.
 * @member {Array.<String>} urls
 */
InstitutionalCertificate.prototype['urls'] = undefined;

/**
 * @member {String} @id
 */
InstitutionalCertificate.prototype['@id'] = undefined;

/**
 * @member {Array.<String>} @type
 */
InstitutionalCertificate.prototype['@type'] = undefined;

/**
 * A summary of the object.
 * @member {String} summary
 */
InstitutionalCertificate.prototype['summary'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
InstitutionalCertificate['StatusEnum'] = {

    /**
     * value: "archived"
     * @const
     */
    "archived": "archived",

    /**
     * value: "deleted"
     * @const
     */
    "deleted": "deleted",

    /**
     * value: "in progress"
     * @const
     */
    "in progress": "in progress",

    /**
     * value: "released"
     * @const
     */
    "released": "released"
};


/**
 * Allowed values for the <code>data_use_limitation</code> property.
 * @enum {String}
 * @readonly
 */
InstitutionalCertificate['DataUseLimitationEnum'] = {

    /**
     * value: "DS"
     * @const
     */
    "DS": "DS",

    /**
     * value: "GRU"
     * @const
     */
    "GRU": "GRU",

    /**
     * value: "HMB"
     * @const
     */
    "HMB": "HMB",

    /**
     * value: "other"
     * @const
     */
    "other": "other"
};


/**
 * Allowed values for the <code>dataUseLimitationModifiers</code> property.
 * @enum {String}
 * @readonly
 */
InstitutionalCertificate['DataUseLimitationModifiersEnum'] = {

    /**
     * value: "COL"
     * @const
     */
    "COL": "COL",

    /**
     * value: "GSO"
     * @const
     */
    "GSO": "GSO",

    /**
     * value: "IRB"
     * @const
     */
    "IRB": "IRB",

    /**
     * value: "MDS"
     * @const
     */
    "MDS": "MDS",

    /**
     * value: "NPU"
     * @const
     */
    "NPU": "NPU",

    /**
     * value: "PUB"
     * @const
     */
    "PUB": "PUB"
};



export default InstitutionalCertificate;

